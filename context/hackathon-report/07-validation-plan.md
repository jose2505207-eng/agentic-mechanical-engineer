# Validation Plan

- **Unit:** schema validation tests per agent node
- **Geometry:** every template renders + exports watertight STL across its full parameter range (automated sweep)
- **Physics:** known-answer checks (e.g., a deliberately top-heavy config must fail the tip-over test)
- **End-to-end:** 3 canned prompts run clean before demo recording
