"""Pipeline agents.

MVP: each "agent" is a deterministic, testable function with a typed
input/output contract. The AI layer (app/llm) can replace any of them later
behind the same contract — that is the whole upgrade strategy.
"""
